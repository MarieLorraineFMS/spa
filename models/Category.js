export class Category {
    constructor({ id, name, color, icon, imageUrl }) {
        this.id = id;
        this.name = name;
        this.color = color;
        this.icon = icon;
        this.imageUrl = imageUrl;
    }
}
